defmodule Leds.Mixfile do

  use Mix.Project

  def project, do: [
    app: :leds,
    version: "0.2.0",
    elixir: "~> 1.0",
    deps: deps
  ]

  def application, do: [ ]

  defp deps, do: []

end
